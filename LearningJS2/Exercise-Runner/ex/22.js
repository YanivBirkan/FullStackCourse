// 22. Write a function named myPow which receives 2 parameters: base, exponent
function myPow(base , exponent){
    sum = Math.pow(base,exponent);
    return sum;
}

console.log(`result = ${myPow(2,3)}`);